FROM chia-gaming-web
# Note: The docker images are built up in this order:
# Dockerfile.rust, Dockerfile.web, { Dockerfile.test, Dockerfile }
# TODO: run the production version of chia-gaming,
#       using the testnet blockchain
