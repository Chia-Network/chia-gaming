;; return a prepending of an agg_sig_unsafe of the
;; shatree of the conditions to the curried pubkey.

(include *standard-cl-23*)
(import std.condition_codes)
(import std.shatree)

(export (PUBKEY conditions)
  (c (list AGG_SIG_UNSAFE PUBKEY (shatree conditions)) conditions)
  )

