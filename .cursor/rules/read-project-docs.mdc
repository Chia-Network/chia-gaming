---
description: MANDATORY first action — read project docs before ANY response
alwaysApply: true
---

# MANDATORY: Read Project Docs First

**Your FIRST tool call in every conversation MUST be reading these two files. No exceptions. Do not respond to the user's message, do not think about the task, do not do anything else until both files have been read.**

1. `ARCHITECTURE.MD`
2. `DEBUGGING_GUIDE.md`

Read both files in your very first response, before addressing anything the user said.

**Build/test commands:** Always use `./cb.sh` and `./ct.sh`. Never use `cargo test` directly.
