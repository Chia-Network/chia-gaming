
(
    ; returns the first count elements of mylist
    (defun slice (mylist count)
        (if (not (print (list "slice" mylist count) count))
            0
            (c (f mylist) (slice (r mylist) (- count 1)))
        )
    )
)
